// Unicode substition cipher

var SG = {};

SG.subglyphIt = function(el, options) {
  // iterate over an element and replace text with selected dictionary
  // -

};

SG.toggleSubglyph = function() {
  // change font of elements marked with ~ 'data-subglyph'

};
