import { ShaderPrefixProcessor } from '../../lib/shader_prefix.js';

describe('ShaderPrefixProcessor', () => {
  it('can dynamically generate prefixes for shaders', () => {
    expect(3).to.equal(3);
  });
});
