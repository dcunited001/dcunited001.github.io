import test from 'tape';
// import { ShaderPrefixProcessor } from '../../lib/shader_prefix.js';

console.log("testing: prefix");

test("can dynamically generate prefixes for shaders", function(t) {
  // t.plan(1);


  // t.equals('foo', 'foo', '...');
  t.end();
});
